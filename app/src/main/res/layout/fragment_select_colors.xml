<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_marginTop="@dimen/double_margin"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_height="wrap_content" />

    <TextView
            android:id="@+id/selectedColorsHeader"
            android:layout_marginTop="48dp"
            android:layout_marginStart="@dimen/double_margin"
            android:text="@string/selected_colors"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/recyclerView"
            style="@style/HeaderText" />

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/selectedColorsRecyclerView"
            android:layout_width="match_parent"
            android:layout_marginTop="@dimen/double_margin"
            app:layout_constraintTop_toBottomOf="@id/selectedColorsHeader"
            android:layout_height="wrap_content" />

    <TextView
            android:id="@+id/nextButton"
            style="@style/BigButton.AccentTheme"
            android:layout_width="@dimen/next_button_width"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/double_margin"
            android:text="@string/next"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>